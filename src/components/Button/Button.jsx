import {StyledLoadButton} from './StyledLoadButton.styled'


export const Button = ({onClick}) => {
    return (
        <StyledLoadButton type='button' onClick={onClick}>Load more</StyledLoadButton>
        
    )
 }

